# Runtime (non-ML) dependencies: API, configuration, extraction, indexing I/O.
# ML/embedding dependencies live in requirements-ml.txt (lazy-imported at
# runtime); test and quality tooling lives in requirements-dev.txt.
fastapi==0.133.1
uvicorn[standard]==0.41.0
openai==2.24.0
pydantic==2.12.5
pydantic-settings>=2.10.1,<3.0.0
opensearch-py==3.1.0
python-dotenv==1.2.1
python-multipart>=0.0.9
prometheus-client>=0.25.0,<1.0.0

ifcopenshell==0.8.3.post1

pillow>=10.0.0
numpy>=1.26.0
tqdm==4.67.1

# HBIM-030: HTTP client for the isolated Qwen3 embedding service (production dep).
httpx~=0.28.1

# HBIM-070 §7: born-digital PDF ingestion. MIT. Deliberately the minimal
# extras — `models-local` (torch/HuggingFace/docling-ibm-models) is NOT
# pulled, so the PDFium backend runs offline with no model weights.
docling-slim[convert-core,format-pdf-pypdfium2]==2.115.0

# HBIM-082 §10 — official Neo4j Python driver. 6.x supports server 4.4/5.x/2025.x;
# the deployment server is Community 5.26.0 (LTS). No other graph dependency.
neo4j~=6.2.0
